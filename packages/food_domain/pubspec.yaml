name: food_domain
description: GlyLens food intelligence domain contracts and value objects.
publish_to: none
version: 1.1.0

environment:
  sdk: ^3.12.0

dependencies:
  equatable: ^2.0.7
  meta: ^1.15.0
  shared_core:
    path: ../shared_core
  shared_models:
    path: ../shared_models

dev_dependencies:
  lints: ^5.1.1
  test: ^1.25.8
