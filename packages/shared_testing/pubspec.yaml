name: shared_testing
description: Test fixtures, mothers, and fakes for GlyLens domain packages.
publish_to: none
version: 1.1.0

environment:
  sdk: ^3.12.0

dependencies:
  food_domain:
    path: ../food_domain
  shared_core:
    path: ../shared_core
  shared_models:
    path: ../shared_models
  test: ^1.25.8

dev_dependencies:
  lints: ^5.1.1
